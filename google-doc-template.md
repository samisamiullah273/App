# n8n Assignment 04 — Daily News Summary Automation

## 1. Short description
This workflow automatically fetches the latest news articles and uses an AI node to summarise them into a concise daily email digest. It solves the real problem of checking multiple news sources by delivering a single actionable summary.

## 2. Workflow canvas screenshot
*(Paste your full workflow screenshot here, showing connected nodes.)*

## 3. Successful execution proof
*(Paste a screenshot or execution log from a successful run here.)*

## 4. Exported workflow JSON
Paste the full JSON below from the n8n workflow export.

```json
<PASTE WORKFLOW JSON HERE>
```

---

## Notes
- This Google Doc is set to "Anyone with the link" as required.
- The workflow includes:
  - `Schedule Trigger` (trigger)
  - `Fetch Latest News` (API action)
  - `AI Summarize` (AI node)
  - `Send Digest Email` (output action)

## Public link verification
- Confirm the link works in a private/incognito browser window.
